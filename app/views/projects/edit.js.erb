<% if defined?(error) && error %>
$("#modal-form-project").html("<%= escape_javascript(render 'shared/error', msg: msg ) %>");

<% else %>
$("#modal-form-project").html("<%= escape_javascript(render 'form') %>");

<% end %>
